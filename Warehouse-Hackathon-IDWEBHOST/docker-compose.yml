version: '3.8'

services:
  warehouse-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aura-warehouse-frontend
    restart: unless-stopped
    ports:
      - "3002:80"
    environment:
      - NODE_ENV=production
