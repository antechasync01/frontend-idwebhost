version: '3.8'

services:
  owner-dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aura-owner-dashboard
    restart: always
    ports:
      - "3009:80"
    environment:
      - NODE_ENV=production
