import { AppRegistry } from 'react-native';
import App from './App';

AppRegistry.registerComponent('AuraSmartOperations', () => App);
AppRegistry.runApplication('AuraSmartOperations', {
  rootTag: document.getElementById('root'),
});
